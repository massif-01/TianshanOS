# v0.5.2 提交与发布验证

基于上游 `d6ed947a592265fa12754828bc79803fc50c1db2`，合并证书/HTTPS、自动化/SSH/日志与 WebUI 提示修复。保留原有设备策略、分区和权限；没有设备连接、刷机、重启或模型启动/停止。

## 提交前验证

- `./tests/runtime/run_all.sh`：生产 codec/store/engine/SSH/service/watcher/log 等主机回归通过；含持久化故障注入、取消/代次/实例身份与实际 UI 函数。C 使用 ASan/UBSan。
- `./tests/certificate/run_host.sh`：真实 SDK mbedTLS/cJSON 与平台替身测试通过；覆盖材料、时间、HTTPS 生命周期、协调、API 及服务停止失败。
- `./tests/certificate/test_service_stop.sh`：原缺陷固定基线为 d6ed947；不再以变化的 HEAD 当旧代码，提交后仍能执行。
- `npm test --prefix tests/prompts`：38/38；`npm run test:browser --prefix tests/prompts`：36/36。
- `python3 tests/release/test_release.py`：3 项通过，覆盖实际发布条件矩阵、版本不一致、缺失/非中文说明的拒绝，以及提交绑定/构建依赖。
- `./tools/build.sh --fresh`：ESP-IDF v5.5.2 / ESP32-S3 完整构建通过。应用大小 0x20fba0，最小应用分区 0x300000，剩余 31%。WebUI 重新优化并生成 SPIFFS 镜像。
- `git diff --check`、JavaScript 语法、Workflow YAML 解析通过。

本地构建的元数据包含提交前工作区标志。正式 Release 由 Actions 从合并后的具体提交重新构建，不直接将本地提交前镜像作为云端验收结果。

## 发布条件

- Fork 分支和上游 PR 执行构建/回归，不发布正式版本。
- 上游 `RMinte-AI/TianshanOS` 的 main 合并推送，构建与 WebUI 回归均成功后，依据 version.txt 自动创建 v0.5.2。
- Release 绑定触发构建的 github.sha，中文正文来自 `docs/releases/v0.5.2.md`，不使用自动生成的英文提交摘要替代正文。
- tools/check_release.py 要求 tag、version.txt、构建版本一致，并检查中文说明。没有对应说明的新版本不能静默发布。
- 若同版本 Release 已存在，main 流程保持既有的跳过行为，不覆盖正式版本。发布新版本需同步更新版本与说明。
- 本轮仅提交和提 PR，不自行合并，也不提前创建 v0.5.2 标签或 Release。

## 残余验证

主机模拟与云端编译无法证明真实 TLS 握手、设备会话吊销、断电/NVS 页回收、历史配置升级、Linux 进程组、SFTP/SCP、DNS 取消边界、三模型联动及长期资源趋势。具体矩阵见 `docs/CERTIFICATE_FIX_REPORT.md` 与 `docs/runtime-repair/VERIFICATION.md`。这些限制在中文发布说明中保留。

## 2026-09-25：未合并 PR 的 WebSocket 增量

本节更新上述首次提交记录；版本和发布触发条件不变。新增按需采集、定向发送、事件节点保活/排空，以及 R1—R5 停止重试、SSH 终态预留、日志代次和公平调度修复。

- 本轮提交前再次执行 `bash tests/ws_subscriptions/run_reviewer.sh`、`bash tests/ws_subscriptions/run_host.sh` 与 `python3 tests/release/test_release.py`；实际生产 C 回归与发布条件检查通过。
- 修复前的未提交源码已归档并校验哈希，`run_reviewer_baseline.sh` 可重放五项反例。当前代码回归检查最终模拟发送边界，未以独立逻辑副本或测试数量代替行为验收。
- 修复后运行时、证书及前端回归通过；Node 38 项、Chrome 中英文 38 项。浏览器使用模拟服务，不是与设备端 C 联机测试。
- 最终生产源码由 ESP-IDF v5.5.2 在隔离构建目录编译链接通过：镜像 `0x213450` 字节，最小应用分区仍余 31%。提交前检查源文件哈希与这次构建证据一致；本轮更新发布文档未改动生产源码。
- 完整行为、命令、日志、基线与设备验收清单见 [Reviewer 修复记录](../repair/ws-subscriptions/reviewer-fixes/README.md)。其中“未提交/未推送”描述修复交付时点；本次按用户后续授权提交并更新原 PR。
- 新 PR head 的 Actions 结果须单独查看，不沿用旧 head `35238b4` 的绿灯。PR 事件只构建，合并 main 后才有发布资格。

这些主机与构建结果不证明真实 TCP、ESP32 双核并发、最大设备负载、长期堆碎片和栈水位已验收；现有 `/ws` 独立认证/主题授权缺口不属于本轮修复。
